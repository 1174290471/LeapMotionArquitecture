using UnityEngine;
using System.Collections;

public class CutObject : MonoBehaviour
{/*
	public HandController hand_controller;
	private HandModel[] graphicsHands;
	private HandModel[] physicsHands;

	private GameObject myCube; 

	private string left = "LEFT";
	private string right = "RIGHT";
	private int thumb = 0;
	private int index = 1;
	private int middle = 2;
	private int ring = 3;
	private int pinky = 4;

	// Use this for initialization
	void Start ()
	{
		myCube = GameObject.Find ("Cube");
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}

	float getIndexPosition(GameObject figure, string type){
		graphicsHands = hand_controller.GetAllGraphicsHands ();
		physicsHands = hand_controller.GetAllPhysicsHands ();

		if(graphicsHands.Length >= 1){
			Vector3 finger;
			Vector3 figurePosition;

			for (int i = 0; i < graphicsHands.Length; i++) {
				if (graphicsHands [i].IsRight() && type.Equals("Right")) {
					finger = graphicsHands [i].fingers [1].GetTipPosition ();
				}		
			}
		}
		return finger;
	}*/
}
